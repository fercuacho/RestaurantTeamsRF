package com.example.restaurantteamsrf.calendar.ui.activities

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MuestraFechas : AppCompatActivity() {
    //private lateinit var binding: ActivityMuestraFechasBinding
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        /*binding = ActivityMuestraFechasBinding.inflate(layoutInflater)
        setContentView(binding.root)


        val selectedDates = intent.getStringArrayListExtra("selectedDates")

        binding.tvDates.text = selectedDates.toString()*/
    }
}